# Backend Production Environment
# Diese Datei wird bei Railway Deployment verwendet
# WICHTIG: Diese Werte werden von Railway Environment Variables überschrieben!
# KEINE ECHTEN SECRETS HIER! Nur Platzhalter für Dokumentation.

# Server Konfiguration
NODE_ENV=production
PORT=5000

# MongoDB Atlas (MUSS in Railway gesetzt werden!)
MONGODB_URI=your-mongodb-connection-string-here
DATABASE_MODE=production

# JWT Konfiguration (MUSS in Railway gesetzt werden!)
JWT_SECRET=your-secure-jwt-secret-here-min-32-chars
JWT_EXPIRE=7d

# CORS - Frontend URLs (Production - MUSS in Railway gesetzt werden!)
FRONTEND_URL=https://your-vercel-domain.vercel.app

# Admin Konfiguration (MUSS in Railway gesetzt werden!)
ADMIN_EMAIL=your-admin-email@example.com
ADMIN_PASSWORD=your-secure-admin-password

# Upload Konfiguration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=/app/uploads

# PayPal Configuration (MUSS in Railway/Vercel Environment Variables gesetzt werden!)
# Diese Werte sind nur Platzhalter - echte Credentials werden über Railway/Vercel Environment Variables injiziert
# 
# Erforderliche Environment Variables:
# PAYPAL_SANDBOX_CLIENT_ID=your-sandbox-client-id-here
# PAYPAL_SANDBOX_CLIENT_SECRET=your-sandbox-client-secret-here
# PAYPAL_LIVE_CLIENT_ID=your-live-client-id-here
# PAYPAL_LIVE_CLIENT_SECRET=your-live-client-secret-here
#
# Legacy Support (optional):
# PAYPAL_CLIENT_ID=fallback-to-sandbox-client-id
# PAYPAL_CLIENT_SECRET=fallback-to-sandbox-client-secret
